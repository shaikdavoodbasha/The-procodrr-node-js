core node js modules

import dgram from "node:dgram" //UDP
import net from "node:net"//TCP
import http from "node:http"
import https from "node:https"
import dns from "node:dns"

socket is also a object which helps to receive the message from client
browser only understands the http application,not UDP AND TCP



what is TCP protocol
what is socket:
a communication end point
tcp sockets verifys the data 
udp sockets just sends the data not verify



File transferring using UDP
when ever we connect our clien to our server will start a three way handshake


http - methods 

GET - retrives data from the server
post - sends data to the server to create new resource
put - updates or creates a  resource at a specific URL
delete - deletes the specified resource from the server
head - same as get but only retrievs headers
options - describes communication options for a resource
patch - partically updates a resource at a specific URL.


HTTP Response status codes


http request and http response anotomy
