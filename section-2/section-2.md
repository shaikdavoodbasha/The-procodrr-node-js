Basics of terminal. section-2

what is terminal?
terminal is a app ,like browser,

In the terminal we can be able to use the mutiple shells like
1)powershell updated using the nano text editor
2)command promptupdated using vim editor
3)azure shell
like in the browser we are using the different websites same like that in the terminal we are using the shells.
bash shell is very important for the backend developers and in the macos it has zsh shell.

why bash shell is important becuase when we deploy the application to the server then we should only have the access to the bash shell only.
so that we need to use bash shell only.

command in the bash
echo command is like console log.
bash is like a scripting language we can create variable and use loops.

commands
pwd - print working directory
whoami - 
cd - change directory

~ - this is the home directory
ls - list of files 
ls -a give all the files.

mkdir - create directories
touch - to create files
terminal has lot uses then the GUI
mv - move
rmkdir - remove directory

Editing the files and reading the files in the terminal
commands 
cat - to read the files

best text editors 
nano editor 
vim editor 
vim is very powerful editor which is used a lot to exit from vim editory need to click firs esc button and shitf+ colon and q 
for saving the file shift+:w to save the file.
without saving shift+q!

what are prompt in terminal??
the whole path is called prompt of the termial example
C:\Users\shaik\Desktop\The procodrr node js\davoodbhai--ps

